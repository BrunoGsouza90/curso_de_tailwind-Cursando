// tailwind.config.js

module.exports = {
  darkMode: 'class', // Habilita o modo escuro com base na classe HTML
  theme: {
    extend: {
      colors: {
        // Defina suas cores personalizadas aqui, se necessário
        // Por exemplo, para uma cor de fundo escura
        dark: '#374151',
      },
    },
  },
  plugins: [],
};